# GUI 控制面板 阶段1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 florr-auto-pathing 加一个 CustomTkinter 深色 GUI —— 侧栏导航、右下角大「自动检测 AFK」开关、控制台页（地图选点/框刷怪区、时长、索敌 AI 开关、实时日志、一键启停），配置落到 exe 同级 `config.json`，机器人以 `--worker` 子进程运行。

**Architecture:** `main.py` 加 `argparse`：无参 → 起 GUI（`gui_app.main()`），`--worker` → 现有刷怪循环（搬进 `run_worker(cfg)`）。GUI 进程点「开始」时做交互引导（Chrome/AFK 模态框）→ 写 `config.json` → `subprocess.Popen` 拉起 worker 并把它的 stdout 灌进日志面板；「停止」发信号让 worker 先 `reset_keyboard()` 再退出。配置读写集中在 `app_config.py`，GUI 与 worker 共用。

**Tech Stack:** Python 3、CustomTkinter（+ 内嵌 `tkinter.Canvas` 做地图选择器）、Pillow（已有依赖）、`subprocess` + 守护线程做进程/日志、pytest + `unittest.mock`/`monkeypatch` 测试。

## Global Constraints

- 唯一新增依赖：`customtkinter`（纯 Python，带 `darkdetect`/`packaging` 小依赖）。不新增 Qt/webview 等重依赖。`numpy<2` / `opencv-python<5` / `torch<2.3` 约束不动。
- 所有「跟可执行文件同级」的路径一律用 `os.path.dirname(os.path.abspath(sys.argv[0]))`（沿用 `cdp_bridge._CHROME_PROFILE_DIR`、`afk_watch._INSTALL_ROOT` 的既有约定）。
- 注释和用户可见的 `print` 文案用中文，跟仓库现有「密集写清 why」的注释风格一致。
- 测试：pytest；一切外部 I/O（`urllib`、`subprocess`、文件系统用 `tmp_path`）全部 mock，绝不依赖「这台机器上是否有 Chrome / 端口 / 显示器」这种外部状态。平台相关代码用 `sys.platform` 判断，测试里用 `monkeypatch.setattr(<模块>.sys, "platform", "win32")` 切换。
- 每个绿色步骤后立刻 commit。Conventional Commits。commit message 结尾加 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`。
- 非 win32 平台：florr-auto-afk 相关函数全部 no-op；GUI 里 AFK 开关置灰。
- `main.py` 顶部 `from utils import *`（无 `__all__`，会带进 `sys`/`os`/`time`/`math`/`pyautogui` 等）。新加的顶层 `import` 要显式写出（`import sys` / `import signal` / `import argparse` / `import app_config`），不靠 star import。
- `import main` 必须保持无副作用（`test_main_smoke.py` 依赖它）：argparse 只在 `if __name__ == "__main__":` 里跑，GUI 的 import 放在该分支内部惰性 import。

---

### Task 1: `app_config.py` —— 配置读写（GUI 与 worker 共用）

**Files:**
- Create: `app_config.py`
- Test: `test_app_config.py`

**Interfaces:**
- Consumes: 无
- Produces:
  - `app_config.DEFAULTS: dict` —— 键与默认值见下
  - `app_config.CONFIG_PATH: str`
  - `app_config.load_config() -> dict` —— 读 `CONFIG_PATH`，缺文件/坏内容回落 `DEFAULTS`，逐键校验
  - `app_config.save_config(cfg: dict) -> None` —— 先 `_coerce` 再写（`json.dump(indent=2, ensure_ascii=False)`）
  - 校验后的 dict 键：`map:str`、`location:list[int,int]`、`farming_area:list[list[int,int]]`（2 个角）、`farming_duration:int`、`consecutive_short_round_limit:int`、`enemy_ai_enabled:bool`、`auto_switch_server:bool`、`afk_enabled:bool`

- [ ] **Step 1: 写失败测试**

Create `test_app_config.py`:

```python
import copy
import json

import pytest

import app_config


@pytest.fixture
def cfg_path(tmp_path, monkeypatch):
    p = tmp_path / "config.json"
    monkeypatch.setattr(app_config, "CONFIG_PATH", str(p))
    return p


def test_load_missing_file_returns_defaults(cfg_path):
    assert app_config.load_config() == app_config.DEFAULTS
    # 返回的必须是副本, 改它不能污染 DEFAULTS
    got = app_config.load_config()
    got["farming_duration"] = 999
    assert app_config.DEFAULTS["farming_duration"] != 999


def test_save_then_load_roundtrips(cfg_path):
    cfg = copy.deepcopy(app_config.DEFAULTS)
    cfg["map"] = "ocean"
    cfg["location"] = [100, 120]
    cfg["farming_area"] = [[10, 10], [40, 40]]
    cfg["enemy_ai_enabled"] = False
    app_config.save_config(cfg)
    assert app_config.load_config() == cfg


def test_bad_json_falls_back_to_defaults(cfg_path):
    cfg_path.write_text("{ not json", encoding="utf-8")
    assert app_config.load_config() == app_config.DEFAULTS


def test_partial_file_fills_missing_keys(cfg_path):
    cfg_path.write_text(json.dumps({"map": "anthell"}), encoding="utf-8")
    got = app_config.load_config()
    assert got["map"] == "anthell"
    assert got["farming_duration"] == app_config.DEFAULTS["farming_duration"]


@pytest.mark.parametrize("bad", [
    {"map": "nonsense"},
    {"map": 123},
    {"location": [1, 2, 3]},
    {"location": "12,32"},
    {"farming_area": [[1, 2], [3, 4], [5, 6]]},
    {"farming_area": [[1, 2], [3]]},
    {"farming_duration": -5},
    {"farming_duration": "300"},
    {"consecutive_short_round_limit": 0},
    {"enemy_ai_enabled": "yes"},
])
def test_bad_value_reverts_that_key_to_default(cfg_path, bad):
    key = next(iter(bad))
    cfg_path.write_text(json.dumps(bad), encoding="utf-8")
    got = app_config.load_config()
    assert got[key] == app_config.DEFAULTS[key]


def test_top_level_not_dict_falls_back(cfg_path):
    cfg_path.write_text("[1, 2, 3]", encoding="utf-8")
    assert app_config.load_config() == app_config.DEFAULTS


def test_unknown_keys_are_dropped(cfg_path):
    cfg_path.write_text(json.dumps({"map": "desert", "bogus": 1}), encoding="utf-8")
    assert "bogus" not in app_config.load_config()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_app_config.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app_config'`

- [ ] **Step 3: 写实现**

Create `app_config.py`:

```python
"""GUI 与 worker 共用的配置读写. main.py 以前把这些值硬编码在 __main__ 里,
现在集中到 exe 同级的 config.json —— GUI 写, worker(`main.py --worker`)读.

设计要点跟 afk_watch._write_afk_config() 一致: 读不出来/键坏了不抛, 回落到
DEFAULTS(= 以前硬编码那套) + print 一句警告. 这里只读不写用户那份坏文件,
所以不需要像 afk_watch 那样备份 .bak.
"""
import copy
import json
import os
import sys

CONFIG_PATH = os.path.join(
    os.path.dirname(os.path.abspath(sys.argv[0])), "config.json"
)

# 每个值 = 改造前 main.py __main__ 里的硬编码值.
DEFAULTS = {
    "map": "desert",
    "location": [22, 32],
    "farming_area": [[9, 8], [51, 56]],
    "farming_duration": 300,
    "consecutive_short_round_limit": 2,
    "enemy_ai_enabled": True,
    "auto_switch_server": True,
    "afk_enabled": False,
}

# maps/ 下的 3 个 png(去扩展名). 新增地图要同步这里 —— 跟 utils.check_map_border()
# 那种"就地写死一小张表"的仓库既有做法一致, 不在 import 时去 listdir.
_VALID_MAPS = ("desert", "ocean", "anthell")


def _is_int_pair(v):
    return (
        isinstance(v, (list, tuple))
        and len(v) == 2
        and all(isinstance(n, int) and not isinstance(n, bool) for n in v)
    )


def _coerce(raw):
    """拿 DEFAULTS 当底, 逐键把 raw 里合法的值覆盖上去; 不合法的键留默认 + 警告.
    未知键直接丢掉(worker 只认 DEFAULTS 里那几个键)."""
    cfg = copy.deepcopy(DEFAULTS)
    if not isinstance(raw, dict):
        print(f"⚠️ config.json 顶层不是对象(是 {type(raw).__name__}), 全部用默认值")
        return cfg

    for key, default in DEFAULTS.items():
        if key not in raw:
            continue
        val = raw[key]
        ok = False
        if key == "map":
            ok = isinstance(val, str) and val in _VALID_MAPS
        elif key == "location":
            ok = _is_int_pair(val)
            if ok:
                val = [int(val[0]), int(val[1])]
        elif key == "farming_area":
            ok = (
                isinstance(val, (list, tuple))
                and len(val) == 2
                and all(_is_int_pair(corner) for corner in val)
            )
            if ok:
                val = [[int(c[0]), int(c[1])] for c in val]
        elif key == "farming_duration":
            ok = isinstance(val, int) and not isinstance(val, bool) and val > 0
        elif key == "consecutive_short_round_limit":
            ok = isinstance(val, int) and not isinstance(val, bool) and val >= 1
        else:  # enemy_ai_enabled / auto_switch_server / afk_enabled
            ok = isinstance(val, bool)

        if ok:
            cfg[key] = val
        else:
            print(f"⚠️ config.json 的 {key} 值不合法({val!r}), 用默认 {default!r}")

    return cfg


def load_config():
    try:
        with open(CONFIG_PATH, "r", encoding="utf-8") as f:
            raw = json.load(f)
    except FileNotFoundError:
        return copy.deepcopy(DEFAULTS)
    except Exception as e:
        print(f"⚠️ 读 config.json 失败, 全部用默认值: {e}")
        return copy.deepcopy(DEFAULTS)
    return _coerce(raw)


def save_config(cfg):
    """写之前先 _coerce, GUI 传进来的东西也不例外 —— 别让界面 bug 写出一份坏配置."""
    cleaned = _coerce(cfg)
    with open(CONFIG_PATH, "w", encoding="utf-8") as f:
        json.dump(cleaned, f, indent=2, ensure_ascii=False)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_app_config.py -v`
Expected: PASS（全部）

- [ ] **Step 5: Commit**

```bash
git add app_config.py test_app_config.py
git commit -m "$(printf 'feat: add app_config for shared GUI/worker config\n\nConfig moves out of main.py hardcoded __main__ into an exe-level\nconfig.json. load_config/save_config coerce every key, reverting bad or\nmissing values to the pre-refactor defaults with a warning.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 2: `cdp_bridge.py` —— 拆出非交互的公有函数

**Files:**
- Modify: `cdp_bridge.py`（新增 3 个公有函数；`launch_dedicated_chrome()` 改成调它们去重）
- Test: `test_cdp_bridge.py`（追加）

**Interfaces:**
- Consumes: 现有私有 `_quit_all_chrome()`、`_launch_chrome_process()`、`_poll_for_florr_tab(timeout, interval=1)`、`eval_js(expr, timeout=5)`、`_is_cdp_port_reachable()`
- Produces:
  - `cdp_bridge.is_dedicated_chrome_ready() -> bool` —— 端到端探测（`eval_js("1")` 不抛就算就绪），任何异常吞掉返 `False`
  - `cdp_bridge.quit_and_launch_chrome() -> None` —— 非交互：杀 Chrome + 带三参数拉起；`_launch_chrome_process()` 找不到 Chrome 时抛 `RuntimeError`
  - `cdp_bridge.wait_for_florr_tab(timeout: float, interval: float = 1.0) -> dict | None` —— 轮询到找到 florr.io 标签页或超时（超时返 `None`，不抛）

- [ ] **Step 1: 写失败测试**（追加到 `test_cdp_bridge.py` 末尾）

```python
def test_is_dedicated_chrome_ready_true_when_eval_succeeds():
    with patch("cdp_bridge.eval_js", return_value={"result": {}}):
        assert cdp_bridge.is_dedicated_chrome_ready() is True


def test_is_dedicated_chrome_ready_false_when_eval_raises():
    with patch("cdp_bridge.eval_js", side_effect=RuntimeError("403")):
        assert cdp_bridge.is_dedicated_chrome_ready() is False


def test_quit_and_launch_chrome_quits_then_launches():
    calls = []
    with patch("cdp_bridge._quit_all_chrome", side_effect=lambda: calls.append("quit")), \
         patch("cdp_bridge._launch_chrome_process", side_effect=lambda: calls.append("launch")):
        cdp_bridge.quit_and_launch_chrome()
    assert calls == ["quit", "launch"]


def test_wait_for_florr_tab_returns_tab_when_found():
    tab = {"url": "https://florr.io/", "webSocketDebuggerUrl": "ws://x"}
    with patch("cdp_bridge.find_florr_tab", return_value=tab):
        assert cdp_bridge.wait_for_florr_tab(5) == tab


def test_wait_for_florr_tab_returns_none_on_timeout():
    with patch("cdp_bridge.find_florr_tab", return_value=None):
        assert cdp_bridge.wait_for_florr_tab(0) is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_cdp_bridge.py -v -k "dedicated or quit_and_launch or wait_for_florr"`
Expected: FAIL —— `AttributeError: module 'cdp_bridge' has no attribute 'is_dedicated_chrome_ready'`

- [ ] **Step 3: 写实现**

在 `cdp_bridge.py` 里，把现有 `_is_dedicated_chrome_ready()` 重命名为公有 `is_dedicated_chrome_ready()`（函数体不变），并新增两个函数。放在 `launch_dedicated_chrome()` 之前：

```python
def is_dedicated_chrome_ready():
    """端到端探一下"专用Chrome已经就绪": CDP端口通 + florr.io标签页开着 +
    WebSocket握手没被403拒(--remote-allow-origins=* 确实给了). 三条全过才算就绪.

    任何异常都吞掉返回False —— 这只是个"能不能省掉重启"的乐观探测, 探不通就
    走完整的杀掉重启流程, 不该由它决定调用方能不能继续.
    """
    try:
        eval_js("1", timeout=3)
        return True
    except Exception:
        return False


def quit_and_launch_chrome():
    """非交互版: 杀掉所有Chrome + 带三个CDP参数拉起专用实例. 不等florr.io标签页
    (调用方用wait_for_florr_tab()自己等). 找不到Chrome可执行文件时
    _launch_chrome_process()会抛RuntimeError, 原样透出去."""
    _quit_all_chrome()
    _launch_chrome_process()


def wait_for_florr_tab(timeout, interval=1.0):
    """轮询find_florr_tab()直到找到florr.io标签页或超时. 超时返回None, 不抛 ——
    调用方(GUI)决定要不要提示重试."""
    return _poll_for_florr_tab(timeout, interval)
```

然后把 `launch_dedicated_chrome()` 里的 `if _is_dedicated_chrome_ready():` 改成 `if is_dedicated_chrome_ready():`，把 `_quit_all_chrome()` + `_launch_chrome_process()` 两行换成 `quit_and_launch_chrome()`，把 `_poll_for_florr_tab(timeout=15)` 换成 `wait_for_florr_tab(15)`。

Run: `grep -rn "_is_dedicated_chrome_ready" .` —— 除了本次改动应无其它引用（`test_cdp_bridge.py` 未 import 它）。若有，一并改成公有名。

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_cdp_bridge.py -v`
Expected: PASS（新老全部）

- [ ] **Step 5: Commit**

```bash
git add cdp_bridge.py test_cdp_bridge.py
git commit -m "$(printf 'refactor: split cdp_bridge into non-interactive public helpers\n\nis_dedicated_chrome_ready / quit_and_launch_chrome / wait_for_florr_tab\nlet the GUI drive the Chrome bootstrap with its own modals instead of\nthe console input() flow. launch_dedicated_chrome now delegates to them.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 3: `afk_watch.py` —— 公有 is-running / stop / download

**Files:**
- Modify: `afk_watch.py`（新增 3 个公有函数，薄封装现有私有实现）
- Test: `test_afk_watch.py`（追加）

**Interfaces:**
- Consumes: 现有 `_is_florr_auto_afk_running()`、`_download_and_extract()`、模块常量 `_EXE_NAME`（`"segment.exe"`）
- Produces:
  - `afk_watch.is_florr_auto_afk_running() -> bool`
  - `afk_watch.stop_florr_auto_afk() -> None` —— `taskkill /IM segment.exe /F`；非 win32 直接 return；吞非零退出码/异常
  - `afk_watch.download_florr_auto_afk() -> bool` —— 不问确认（确认交给 GUI），转调 `_download_and_extract()`

- [ ] **Step 1: 写失败测试**（追加到 `test_afk_watch.py` 末尾）

```python
def test_is_florr_auto_afk_running_delegates(monkeypatch):
    monkeypatch.setattr(afk_watch, "_is_florr_auto_afk_running", lambda: True)
    assert afk_watch.is_florr_auto_afk_running() is True


def test_stop_florr_auto_afk_noop_off_windows(monkeypatch):
    monkeypatch.setattr(afk_watch.sys, "platform", "darwin")
    def boom(*a, **k):
        raise AssertionError("不该在非 Windows 上调 subprocess")
    monkeypatch.setattr(afk_watch.subprocess, "run", boom)
    assert afk_watch.stop_florr_auto_afk() is None


def test_stop_florr_auto_afk_taskkills_on_windows(monkeypatch):
    monkeypatch.setattr(afk_watch.sys, "platform", "win32")
    seen = {}
    monkeypatch.setattr(afk_watch.subprocess, "run",
                        lambda args, **k: seen.setdefault("args", args))
    afk_watch.stop_florr_auto_afk()
    assert seen["args"] == ["taskkill", "/IM", "segment.exe", "/F"]


def test_stop_florr_auto_afk_swallows_errors(monkeypatch):
    monkeypatch.setattr(afk_watch.sys, "platform", "win32")
    monkeypatch.setattr(afk_watch.subprocess, "run",
                        lambda *a, **k: (_ for _ in ()).throw(OSError("boom")))
    afk_watch.stop_florr_auto_afk()  # 不抛即通过


def test_download_florr_auto_afk_delegates_without_prompting(monkeypatch):
    monkeypatch.setattr(afk_watch, "_download_and_extract", lambda: True)
    monkeypatch.setattr(afk_watch, "_prompt_download_confirm",
                        lambda: (_ for _ in ()).throw(AssertionError("不该问确认")))
    assert afk_watch.download_florr_auto_afk() is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_afk_watch.py -v -k "delegates or stop_florr or download_florr"`
Expected: FAIL —— `AttributeError: module 'afk_watch' has no attribute 'is_florr_auto_afk_running'`

- [ ] **Step 3: 写实现**

在 `afk_watch.py` 里 `_is_florr_auto_afk_running()` 定义之后新增：

```python
def is_florr_auto_afk_running():
    """查 segment.exe 是不是已经有实例在跑(只在 Windows 上有意义). GUI 的 AFK
    开关用它决定"要不要再开一个"."""
    return _is_florr_auto_afk_running()


def stop_florr_auto_afk():
    """taskkill 掉 segment.exe(AFK 开关关掉时用). 非 win32 直接返回. 本来就没在
    跑不算失败 —— 吞掉非零退出码和异常, 跟 _is_florr_auto_afk_running() 一个态度."""
    if sys.platform != "win32":
        return
    try:
        subprocess.run(["taskkill", "/IM", _EXE_NAME, "/F"], capture_output=True)
    except Exception as e:
        print(f"⚠️ 停止 florr-auto-afk 失败(忽略): {e}")


def download_florr_auto_afk():
    """下载+校验+解压 florr-auto-afk, 不问确认 —— 确认对话框由 GUI 负责弹.
    命令行那条路(ensure_florr_auto_afk_running)仍然走 _prompt_download_confirm()."""
    return _download_and_extract()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_afk_watch.py -v`
Expected: PASS（新老全部）

- [ ] **Step 5: Commit**

```bash
git add afk_watch.py test_afk_watch.py
git commit -m "$(printf 'feat: add public is-running/stop/download for florr-auto-afk\n\nThe GUI AFK toggle needs to start/kill florr-auto-afk and download it\nwithout the console confirm prompt. Thin wrappers over the existing\nprivate helpers; the console ensure_florr_auto_afk_running path is\nunchanged.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 4: `main.py` —— `run_worker(cfg)` + argparse + 信号处理 + 索敌总开关

**Files:**
- Modify: `main.py`（顶层加 import；`auto_farming` 加 kwarg；抽 `_maybe_scan_enemies`；`__main__` 循环体搬进 `run_worker`；加 `_apply_worker_config`、`_worker_graceful_exit`、`_install_worker_signal_handlers`；新 `__main__` 走 argparse）
- Test: `test_main_worker.py`（新）、`test_main_smoke.py`（追加断言）

**Interfaces:**
- Consumes: `app_config.load_config()`（Task 1）；`cdp_bridge.is_dedicated_chrome_ready()`（Task 2）；现有 `apply_map`、`reset_keyboard`、`enemy_detect.scan_enemies/select_action`、`ENEMY_SCAN_INTERVAL/ENEMY_MODEL_PATH/AVOID_TRIGGER_PX/CAUTIOUS_HOLD_PX`
- Produces:
  - `main._apply_worker_config(cfg: dict) -> dict` —— 调 `apply_map(cfg["map"])`，返回 `{"location": tuple, "farming_area": list[tuple], "farming_duration": int, "short_round_limit": int, "enemy_ai_enabled": bool, "auto_switch_server": bool}`
  - `main._maybe_scan_enemies(enemy_ai_enabled: bool, now: float, last_enemy_scan: float, prev_decision: tuple) -> tuple[tuple, float]` —— 索敌节流 + 总开关；`enemy_ai_enabled=False` 时永远返回 `(("wander", None), last_enemy_scan)` 且不碰 `enemy_detect`
  - `main.auto_farming(farming_area, duration=300, *, enemy_ai_enabled=True) -> bool`
  - `main.run_worker(cfg: dict) -> None` —— 现有主循环（读 cfg 而非硬编码）
  - `main._worker_graceful_exit(signum, frame)` —— `reset_keyboard()` 后 `sys.exit(0)`
  - `main._install_worker_signal_handlers() -> None` —— 挂 `SIGTERM`（+ Windows `SIGBREAK`）

- [ ] **Step 1: 写失败测试**

Create `test_main_worker.py`:

```python
import inspect

import pytest

import main


def test_apply_worker_config_maps_keys(monkeypatch):
    applied = {}
    monkeypatch.setattr(main, "apply_map", lambda name: applied.setdefault("map", name))
    cfg = {
        "map": "ocean",
        "location": [11, 22],
        "farming_area": [[1, 2], [3, 4]],
        "farming_duration": 120,
        "consecutive_short_round_limit": 5,
        "enemy_ai_enabled": False,
        "auto_switch_server": False,
        "afk_enabled": True,
    }
    w = main._apply_worker_config(cfg)
    assert applied["map"] == "ocean"
    assert w["location"] == (11, 22)
    assert w["farming_area"] == [(1, 2), (3, 4)]
    assert w["farming_duration"] == 120
    assert w["short_round_limit"] == 5
    assert w["enemy_ai_enabled"] is False
    assert w["auto_switch_server"] is False


def test_maybe_scan_enemies_disabled_never_touches_enemy_detect(monkeypatch):
    monkeypatch.setattr(main.enemy_detect, "scan_enemies",
                        lambda **k: (_ for _ in ()).throw(AssertionError("不该扫描")))
    decision, last = main._maybe_scan_enemies(False, 1000.0, 0.0, ("chase", "x"))
    assert decision == ("wander", None)
    assert last == 0.0


def test_maybe_scan_enemies_throttled_returns_prev(monkeypatch):
    monkeypatch.setattr(main.enemy_detect, "scan_enemies",
                        lambda **k: (_ for _ in ()).throw(AssertionError("还没到扫描间隔")))
    prev = ("flee", [(1, 2)])
    decision, last = main._maybe_scan_enemies(True, 0.1, 0.0, prev)
    assert decision is prev
    assert last == 0.0


def test_maybe_scan_enemies_scans_when_due(monkeypatch):
    monkeypatch.setattr(main.enemy_detect, "scan_enemies", lambda **k: ["det"])
    monkeypatch.setattr(main.enemy_detect, "select_action",
                        lambda dets, **k: ("chase", "target", 250))
    now = main.ENEMY_SCAN_INTERVAL + 1.0
    decision, last = main._maybe_scan_enemies(True, now, 0.0, ("wander", None))
    assert decision == ("chase", "target", 250)
    assert last == now


def test_maybe_scan_enemies_scan_error_degrades_to_wander(monkeypatch):
    monkeypatch.setattr(main.enemy_detect, "scan_enemies",
                        lambda **k: (_ for _ in ()).throw(RuntimeError("model missing")))
    now = main.ENEMY_SCAN_INTERVAL + 1.0
    decision, last = main._maybe_scan_enemies(True, now, 0.0, ("wander", None))
    assert decision == ("wander", None)
    assert last == now


def test_auto_farming_accepts_enemy_ai_enabled_kwarg():
    sig = inspect.signature(main.auto_farming)
    assert "enemy_ai_enabled" in sig.parameters
    assert sig.parameters["enemy_ai_enabled"].kind == inspect.Parameter.KEYWORD_ONLY


def test_worker_graceful_exit_resets_keyboard_then_exits(monkeypatch):
    called = []
    monkeypatch.setattr(main, "reset_keyboard", lambda: called.append("reset"))
    with pytest.raises(SystemExit):
        main._worker_graceful_exit(15, None)
    assert called == ["reset"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_main_worker.py -v`
Expected: FAIL —— `AttributeError: module 'main' has no attribute '_apply_worker_config'`

- [ ] **Step 3: 写实现**

3a. `main.py` 顶部 import 区加：

```python
import argparse
import signal
import sys

import app_config
```

3b. 把 `auto_farming` 的签名改成关键字参数版，并把里面那段索敌节流块换成调 `_maybe_scan_enemies`。在 `auto_farming` 之前新增 `_maybe_scan_enemies`：

```python
def _maybe_scan_enemies(enemy_ai_enabled, now, last_enemy_scan, prev_decision):
    """索敌节流 + 总开关. 从 auto_farming 里抽出来单测.

    - enemy_ai_enabled=False: 永远返回漫游决策, 一次都不碰 enemy_detect(用户在
      GUI 里关掉了索敌 AI).
    - 距上次扫描不到 ENEMY_SCAN_INTERVAL: 沿用上一轮的决策, 不重复跑 YOLO.
    - 到点了: 跑一次 scan_enemies + select_action; 索敌是附加功能, 任何异常都
      退化成漫游, 不能打断刷怪主循环.

    返回 (decision, last_enemy_scan).
    """
    if not enemy_ai_enabled:
        return ("wander", None), last_enemy_scan
    if now - last_enemy_scan < ENEMY_SCAN_INTERVAL:
        return prev_decision, last_enemy_scan
    last_enemy_scan = now
    try:
        detections = enemy_detect.scan_enemies(model_path=ENEMY_MODEL_PATH)
        decision = enemy_detect.select_action(
            detections,
            avoid_trigger_px=AVOID_TRIGGER_PX,
            cautious_hold_px=CAUTIOUS_HOLD_PX,
        )
    except Exception as e:
        print(f"⚠️ 索敌出错, 本轮当漫游处理: {e}")
        decision = ("wander", None)
    return decision, last_enemy_scan
```

`auto_farming` 签名：`def auto_farming(farming_area, duration=300, *, enemy_ai_enabled=True):`
把原来这段（约在 `now = time.time()` 到 `enemy_action = enemy_decision[0]` 之间）：

```python
        now = time.time()
        if now - last_enemy_scan >= ENEMY_SCAN_INTERVAL:
            last_enemy_scan = now
            try:
                detections = enemy_detect.scan_enemies(model_path=ENEMY_MODEL_PATH)
                enemy_decision = enemy_detect.select_action(
                    detections,
                    avoid_trigger_px=AVOID_TRIGGER_PX,
                    cautious_hold_px=CAUTIOUS_HOLD_PX,
                )
            except Exception as e:
                print(f"⚠️ 索敌出错, 本轮当漫游处理: {e}")
                enemy_decision = ("wander", None)

        enemy_action = enemy_decision[0]
```

换成：

```python
        now = time.time()
        enemy_decision, last_enemy_scan = _maybe_scan_enemies(
            enemy_ai_enabled, now, last_enemy_scan, enemy_decision)
        enemy_action = enemy_decision[0]
```

3c. 把整个 `if __name__ == "__main__":` 块的**循环体**（从 `cdp_bridge.launch_dedicated_chrome()` 那几行之后、`apply_map("desert")` 开始，到文件结尾）搬进新函数 `run_worker(cfg)`，并用 cfg 替换硬编码：

```python
def _apply_worker_config(cfg):
    """把 config.json 的值应用/摊平成 run_worker 主循环要用的局部值.
    apply_map() 必须在这里就调 —— utils 的 MAP 是模块级全局, load_binary_map()
    等一堆函数都读它."""
    apply_map(cfg["map"])
    return {
        "location": tuple(cfg["location"]),
        "farming_area": [tuple(p) for p in cfg["farming_area"]],
        "farming_duration": cfg["farming_duration"],
        "short_round_limit": cfg["consecutive_short_round_limit"],
        "enemy_ai_enabled": cfg["enemy_ai_enabled"],
        "auto_switch_server": cfg["auto_switch_server"],
    }


def run_worker(cfg):
    """刷怪 worker: 由 GUI 以 `main.py --worker` 子进程拉起. 掉线/死亡后自动点
    开始重来, 不主动停(沿用改造前 __main__ 的行为)."""
    # 直接 python main.py --worker 调试时给个清楚的报错 —— 交互式 Chrome 引导
    # 已经搬进 GUI, 这条路不再自己拉 Chrome.
    if not cdp_bridge.is_dedicated_chrome_ready():
        print("❌ 专用 Chrome 未就绪. 请从 GUI 启动(GUI 会引导你准备 Chrome).")
        sys.exit(1)

    afk_watch.ensure_florr_auto_afk_running()
    global overlay
    overlay = create_overlay()

    w = _apply_worker_config(cfg)
    location = w["location"]
    farming_area = w["farming_area"]
    farming_duration = w["farming_duration"]
    CONSECUTIVE_SHORT_ROUND_LIMIT = w["short_round_limit"]

    print("🎮 开始自动寻路+刷怪 (掉线/死亡后自动点开始重来, 不主动停)\n")
    consecutive_short_rounds = 0
    round_count = 0
    while True:
        round_count += 1
        round_start_time = time.time()
        print(f"\n{'='*50}\n第 {round_count} 轮\n{'='*50}")

        if on_death_screen():
            print("💀 检测到死亡结算画面, 点击继续...")
            overlay.update(state="重新开始", message="死亡, 点击继续...")
            click_continue_after_death()
            time.sleep(2)
        if on_start_screen():
            print("🔁 检测到开局菜单, 点击开始按钮进入游戏...")
            overlay.update(state="重新开始", message="点击开始按钮...")
            click_start_game()
            time.sleep(3)

        print(f"📍 目标区域: {farming_area}\n")
        overlay.update(state="启动", target=location,
                       message=f"第{round_count}轮: 开始自动寻路到刷怪区域")

        if lazy_theta_pathing(location, [farming_area]):
            print("✅ 到达刷怪区域！")
            auto_farming(farming_area, farming_duration,
                         enemy_ai_enabled=w["enemy_ai_enabled"])
        else:
            print("❌ 本轮未能到达目标区域")
            overlay.update(message="本轮未能到达目标区域")
            time.sleep(1)

        round_elapsed = time.time() - round_start_time
        completed_full_duration = round_elapsed >= farming_duration
        if completed_full_duration:
            consecutive_short_rounds = 0
        else:
            consecutive_short_rounds += 1
            print(f"⚠️ 这条命只撑了{round_elapsed:.0f}秒, 没到{farming_duration}秒 "
                  f"(连续{consecutive_short_rounds}次)")
            if (w["auto_switch_server"]
                    and consecutive_short_rounds >= CONSECUTIVE_SHORT_ROUND_LIMIT):
                print(f"🌐 连续{consecutive_short_rounds}轮没刷满, 换个服务器...")
                overlay.update(state="换服务器",
                               message=f"连续{consecutive_short_rounds}轮没刷满, 切换中")
                try:
                    switch_server()
                    consecutive_short_rounds = 0
                    time.sleep(2)
                except Exception as e:
                    print(f"⚠️ 换服务器失败, 先用当前服务器继续刷 (下轮再重试): {e}")
                    overlay.update(message=f"换服务器失败(下轮重试): {e}")


def _worker_graceful_exit(signum, frame):
    """GUI 点"停止"时给 worker 发的信号处理: 先把按住的方向键/空格松开, 再退出 ——
    直接 kill 的话角色会带着最后一个鼠标指令继续走."""
    try:
        reset_keyboard()
    finally:
        sys.exit(0)


def _install_worker_signal_handlers():
    signal.signal(signal.SIGTERM, _worker_graceful_exit)
    if hasattr(signal, "SIGBREAK"):  # Windows: GUI 用 CTRL_BREAK_EVENT 打过来
        signal.signal(signal.SIGBREAK, _worker_graceful_exit)
```

3d. 新的 `__main__`：

```python
if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="florr-auto-pathing")
    parser.add_argument("--worker", action="store_true",
                        help="内部用: 跑刷怪循环子进程(由 GUI 拉起, 不要手动加)")
    args = parser.parse_args()

    if args.worker:
        _install_worker_signal_handlers()
        run_worker(app_config.load_config())
    else:
        from gui_app import main as gui_main  # 惰性 import: 不让 `import main` 拖进 GUI 依赖
        gui_main()
```

> 注意：Task 4 完成时 `gui_app` 还不存在，所以**无参跑 `python main.py` 会 ImportError** —— 这是预期的，Task 7 补上 `gui_app`。`--worker` 分支和所有单测这一步就该全绿。

3e. 追加断言到 `test_main_smoke.py`：

```python
    assert hasattr(main, "run_worker")
    assert hasattr(main, "_apply_worker_config")
    assert hasattr(main, "_maybe_scan_enemies")
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_main_worker.py test_main_smoke.py -v`
Expected: PASS

Run（回归）: `pytest test_main_smoke.py test_afk_watch.py test_cdp_bridge.py test_app_config.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add main.py test_main_worker.py test_main_smoke.py
git commit -m "$(printf 'refactor: move the farming loop into run_worker(cfg) with argparse\n\nmain.py --worker runs the loop reading app_config instead of the\nhardcoded __main__ values; no args will launch the GUI (added next\ntask). Enemy detection gets a config on/off gate via the extracted\n_maybe_scan_enemies. SIGTERM/SIGBREAK handler resets the keyboard\nbefore exiting so a GUI stop does not leave keys held.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 5: `gui_map_picker.py` —— 地图选择器（坐标变换纯函数 + `MapPicker` 控件）

**Files:**
- Create: `gui_map_picker.py`
- Modify: `requirements.txt`（加 `customtkinter`）
- Test: `test_gui_map_picker.py`

**Interfaces:**
- Consumes: `customtkinter`、`tkinter`、`PIL`（Pillow，已是依赖）
- Produces:
  - `gui_map_picker.View` —— `@dataclass(frozen=True)`：`s: float`（每图像素在控件里占多少控件像素 = fit×zoom）、`offset_x: float`、`offset_y: float`、`img_w: int`、`img_h: int`
  - `gui_map_picker.image_to_widget(ix: float, iy: float, view: View) -> tuple[float, float]`
  - `gui_map_picker.widget_to_image(wx: float, wy: float, view: View) -> tuple[int, int]` —— 反变换后 `round` 再 clamp 到 `[0, img_w-1] × [0, img_h-1]`
  - `gui_map_picker.clamp_image_point(ix: int, iy: int, img_w: int, img_h: int) -> tuple[int, int]`
  - `gui_map_picker.MapPicker(master, *, on_point_change=None, on_area_change=None)` —— `ctk.CTkFrame` 子类；方法 `.load_map(name: str)`、`.set_point(pt: tuple[int,int] | None)`、`.set_area(area: list[tuple[int,int]] | None)`；用户在图上操作时回调吐出**图像像素**坐标

- [ ] **Step 1: 写失败测试**

Create `test_gui_map_picker.py`:

```python
import pytest

from gui_map_picker import View, image_to_widget, widget_to_image, clamp_image_point


def test_image_to_widget_applies_scale_and_offset():
    v = View(s=2.0, offset_x=10.0, offset_y=20.0, img_w=300, img_h=300)
    assert image_to_widget(0, 0, v) == (10.0, 20.0)
    assert image_to_widget(5, 7, v) == (20.0, 34.0)


def test_widget_to_image_is_inverse_of_image_to_widget():
    v = View(s=1.5, offset_x=12.0, offset_y=8.0, img_w=300, img_h=300)
    for ix, iy in [(0, 0), (150, 150), (299, 299), (33, 210)]:
        wx, wy = image_to_widget(ix, iy, v)
        assert widget_to_image(wx, wy, v) == (ix, iy)


def test_widget_to_image_rounds_to_nearest_pixel():
    v = View(s=4.0, offset_x=0.0, offset_y=0.0, img_w=300, img_h=300)
    assert widget_to_image(9.0, 9.0, v) == (2, 2)   # 9/4 = 2.25 -> 2
    assert widget_to_image(10.0, 10.0, v) == (3, 3)  # 10/4 = 2.5 -> 3(banker's? 用 round)


def test_widget_to_image_clamps_outside_the_image():
    v = View(s=2.0, offset_x=0.0, offset_y=0.0, img_w=300, img_h=300)
    assert widget_to_image(-50.0, -50.0, v) == (0, 0)
    assert widget_to_image(99999.0, 99999.0, v) == (299, 299)


def test_clamp_image_point():
    assert clamp_image_point(-1, 500, 300, 300) == (0, 299)
    assert clamp_image_point(10, 20, 300, 300) == (10, 20)
```

> `round(2.5)` 在 Python 里是 banker's rounding → `2`。第三个测试用例注释说明；把断言写成实际行为：`widget_to_image(10.0,10.0,v)` 期望 `(2,2)`（`round(2.5)==2`）。实现和测试都按 `round` 的真实行为写，不要引特殊处理。

修正该用例：

```python
def test_widget_to_image_rounds_to_nearest_pixel():
    v = View(s=4.0, offset_x=0.0, offset_y=0.0, img_w=300, img_h=300)
    assert widget_to_image(9.0, 9.0, v) == (2, 2)    # 2.25 -> 2
    assert widget_to_image(13.0, 13.0, v) == (3, 3)  # 3.25 -> 3
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_gui_map_picker.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'gui_map_picker'`（或 `customtkinter` 未安装 —— 先做 Step 3 的 requirements + `pip install`）

- [ ] **Step 3: 写实现**

3a. `requirements.txt` 在 `websocket-client` 那行附近加：

```
customtkinter         # gui_app.py 的深色 GUI(纯 Python, 带 darkdetect/packaging)
```

Run: `pip install customtkinter`

3b. Create `gui_map_picker.py`:

```python
"""地图选择器: 在二值地图(maps/<name>.png, 300x300)上左键点=目标点、左键拖=刷怪
矩形、滚轮=1~4x 缩放(视图中心跟随指针, 不做独立拖动平移). 二值图就是寻路用的
坐标系本身(utils.load_binary_map() 读的同一张图), 所以点出来的坐标可以直接进
config.json 的 location / farming_area.

坐标变换(image_to_widget / widget_to_image)抽成模块级纯函数, 不碰 tk, 好单测.
"""
from dataclasses import dataclass

import customtkinter as ctk
import tkinter as tk
from PIL import Image, ImageTk

_MAP_DIR = "./maps"  # 跟 utils.load_binary_map() 一样, cwd 相对(打包后 cwd = exe 目录)
_DRAG_THRESHOLD_PX = 4  # 按下到松开在这个像素内算"点一下"(设目标点), 超过算"拖框"


@dataclass(frozen=True)
class View:
    """当前把 img_w x img_h 的图画到控件里的方式.
    s: 一个图像像素 = 多少控件像素 (= fit * zoom). offset: 图左上角在控件里的坐标.
    """
    s: float
    offset_x: float
    offset_y: float
    img_w: int
    img_h: int


def image_to_widget(ix, iy, view):
    return (view.offset_x + ix * view.s, view.offset_y + iy * view.s)


def clamp_image_point(ix, iy, img_w, img_h):
    return (min(max(ix, 0), img_w - 1), min(max(iy, 0), img_h - 1))


def widget_to_image(wx, wy, view):
    ix = round((wx - view.offset_x) / view.s)
    iy = round((wy - view.offset_y) / view.s)
    return clamp_image_point(ix, iy, view.img_w, view.img_h)


def _fit_scale(canvas_w, canvas_h, img_w, img_h):
    """把整张图塞进控件、保持长宽比的基准缩放."""
    if img_w <= 0 or img_h <= 0:
        return 1.0
    return min(canvas_w / img_w, canvas_h / img_h)


class MapPicker(ctk.CTkFrame):
    def __init__(self, master, *, on_point_change=None, on_area_change=None, **kw):
        super().__init__(master, **kw)
        self._on_point_change = on_point_change
        self._on_area_change = on_area_change
        self._pil = None
        self._tk_img = None
        self._zoom = 1
        self._point = None          # (ix, iy) 图像像素
        self._area = None           # [(ix,iy),(ix,iy)] 图像像素
        self._drag_start = None     # (wx, wy) 按下时的控件坐标

        self._canvas = tk.Canvas(self, highlightthickness=0, bg="#1c1c1e")
        self._canvas.pack(fill="both", expand=True)
        self._canvas.bind("<Configure>", lambda e: self._redraw())
        self._canvas.bind("<Button-1>", self._on_press)
        self._canvas.bind("<B1-Motion>", self._on_drag)
        self._canvas.bind("<ButtonRelease-1>", self._on_release)
        self._canvas.bind("<MouseWheel>", self._on_wheel)          # Win/mac
        self._canvas.bind("<Button-4>", lambda e: self._on_wheel(e, +1))  # X11
        self._canvas.bind("<Button-5>", lambda e: self._on_wheel(e, -1))

    # ---- 公有 API ----
    def load_map(self, name):
        self._pil = Image.open(f"{_MAP_DIR}/{name}.png").convert("RGB")
        self._zoom = 1
        self._redraw()

    def set_point(self, pt):
        self._point = tuple(pt) if pt is not None else None
        self._redraw()

    def set_area(self, area):
        self._area = [tuple(area[0]), tuple(area[1])] if area else None
        self._redraw()

    # ---- 内部 ----
    def _view(self):
        if self._pil is None:
            return None
        cw = max(self._canvas.winfo_width(), 1)
        ch = max(self._canvas.winfo_height(), 1)
        iw, ih = self._pil.size
        s = _fit_scale(cw, ch, iw, ih) * self._zoom
        # 缩放后若比控件大, 让图居中(zoom=1 时正好完全贴合)
        offx = (cw - iw * s) / 2
        offy = (ch - ih * s) / 2
        return View(s=s, offset_x=offx, offset_y=offy, img_w=iw, img_h=ih)

    def _redraw(self):
        self._canvas.delete("all")
        v = self._view()
        if v is None:
            return
        disp_w = max(int(v.img_w * v.s), 1)
        disp_h = max(int(v.img_h * v.s), 1)
        resized = self._pil.resize((disp_w, disp_h))
        self._tk_img = ImageTk.PhotoImage(resized)
        self._canvas.create_image(v.offset_x, v.offset_y, anchor="nw", image=self._tk_img)

        if self._area:
            (x1, y1) = image_to_widget(*self._area[0], v)
            (x2, y2) = image_to_widget(*self._area[1], v)
            self._canvas.create_rectangle(x1, y1, x2, y2, outline="#0a84ff", width=2)
        if self._point:
            (px, py) = image_to_widget(*self._point, v)
            self._canvas.create_line(px - 8, py, px + 8, py, fill="#30d158", width=2)
            self._canvas.create_line(px, py - 8, px, py + 8, fill="#30d158", width=2)

    def _on_press(self, e):
        self._drag_start = (e.x, e.y)

    def _on_drag(self, e):
        if self._drag_start is None:
            return
        v = self._view()
        if v is None:
            return
        a = widget_to_image(*self._drag_start, v)
        b = widget_to_image(e.x, e.y, v)
        self._area = [a, b]
        self._redraw()

    def _on_release(self, e):
        if self._drag_start is None:
            return
        dx = e.x - self._drag_start[0]
        dy = e.y - self._drag_start[1]
        v = self._view()
        self._drag_start = None
        if v is None:
            return
        if (dx * dx + dy * dy) ** 0.5 <= _DRAG_THRESHOLD_PX:
            self._point = widget_to_image(e.x, e.y, v)
            self._area_maybe_clear_none()
            self._redraw()
            if self._on_point_change:
                self._on_point_change(self._point)
        else:
            a = widget_to_image(*(self._drag_start or (e.x, e.y)), v) if self._drag_start else self._area[0]
            self._area = [self._area[0], widget_to_image(e.x, e.y, v)]
            self._redraw()
            if self._on_area_change:
                self._on_area_change([self._area[0], self._area[1]])

    def _area_maybe_clear_none(self):
        pass  # 点目标点不动刷怪区; 占位, 保持 _on_release 可读

    def _on_wheel(self, e, direction=None):
        if direction is None:
            direction = 1 if getattr(e, "delta", 0) > 0 else -1
        self._zoom = min(4, max(1, self._zoom + direction))
        self._redraw()
```

> `_on_release` 里 `self._drag_start` 在函数开头已被读进局部前先判空、末尾置 `None`。上面代码把置 `None` 放在算完 `dx/dy` 之后、用 `v` 之前 —— 注意实现时 **先存 `start = self._drag_start`，再 `self._drag_start = None`，后续都用 `start`**，避免 lint/竞态歧义。按这个顺序重写 `_on_release`：

```python
    def _on_release(self, e):
        start = self._drag_start
        self._drag_start = None
        if start is None:
            return
        v = self._view()
        if v is None:
            return
        dx, dy = e.x - start[0], e.y - start[1]
        if (dx * dx + dy * dy) ** 0.5 <= _DRAG_THRESHOLD_PX:
            self._point = widget_to_image(e.x, e.y, v)
            self._redraw()
            if self._on_point_change:
                self._on_point_change(self._point)
        else:
            self._area = [widget_to_image(*start, v), widget_to_image(e.x, e.y, v)]
            self._redraw()
            if self._on_area_change:
                self._on_area_change(list(self._area))
```

（用这版替换上面初稿里的 `_on_release`，并删掉 `_area_maybe_clear_none`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_gui_map_picker.py -v`
Expected: PASS（5 个用例）

- [ ] **Step 5: Commit**

```bash
git add gui_map_picker.py test_gui_map_picker.py requirements.txt
git commit -m "$(printf 'feat: add MapPicker widget for clicking target / dragging farm area\n\nRenders maps/<name>.png (the binary pathing map, so clicked coords feed\nconfig.json directly). Pure image<->widget coordinate transforms are\nmodule-level and unit-tested; the tk canvas wiring is not.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 6: `gui_chrome_flow.py` —— GUI 里的 Chrome 引导模态流程

**Files:**
- Create: `gui_chrome_flow.py`
- Test: `test_gui_chrome_flow.py`

**Interfaces:**
- Consumes: `cdp_bridge.is_dedicated_chrome_ready()` / `quit_and_launch_chrome()` / `wait_for_florr_tab(timeout)`（Task 2）
- Produces:
  - `gui_chrome_flow.ChromeSetupCancelled(Exception)`
  - `gui_chrome_flow.ensure_chrome_ready(parent, *, confirm=_default_confirm, prompt_retry=_default_prompt_retry) -> None`
    - 已就绪 → 直接返回
    - 未就绪 → `confirm(parent)` 为假 → 抛 `ChromeSetupCancelled`
    - `confirm` 为真 → `quit_and_launch_chrome()` → 循环 `wait_for_florr_tab(15)`；找到 → 返回；没找到 → `prompt_retry(parent)` 为真则再等，为假抛 `ChromeSetupCancelled`
    - `confirm` / `prompt_retry` 是可注入的（默认用 `tkinter.messagebox`），测试传假函数

- [ ] **Step 1: 写失败测试**

Create `test_gui_chrome_flow.py`:

```python
import pytest

import gui_chrome_flow as flow


@pytest.fixture(autouse=True)
def stub_cdp(monkeypatch):
    state = {"ready": False, "launched": 0, "tab_results": []}
    monkeypatch.setattr(flow.cdp_bridge, "is_dedicated_chrome_ready",
                        lambda: state["ready"])
    monkeypatch.setattr(flow.cdp_bridge, "quit_and_launch_chrome",
                        lambda: state.__setitem__("launched", state["launched"] + 1))
    monkeypatch.setattr(flow.cdp_bridge, "wait_for_florr_tab",
                        lambda timeout, *a, **k: state["tab_results"].pop(0))
    return state


def test_returns_immediately_when_already_ready(stub_cdp):
    stub_cdp["ready"] = True
    flow.ensure_chrome_ready(None, confirm=lambda p: pytest.fail("不该问确认"),
                             prompt_retry=lambda p: pytest.fail("不该问重试"))
    assert stub_cdp["launched"] == 0


def test_cancel_at_confirm_raises(stub_cdp):
    with pytest.raises(flow.ChromeSetupCancelled):
        flow.ensure_chrome_ready(None, confirm=lambda p: False,
                                 prompt_retry=lambda p: True)
    assert stub_cdp["launched"] == 0


def test_confirm_then_tab_found_succeeds(stub_cdp):
    stub_cdp["tab_results"] = [{"url": "https://florr.io/"}]
    flow.ensure_chrome_ready(None, confirm=lambda p: True,
                             prompt_retry=lambda p: pytest.fail("不该问重试"))
    assert stub_cdp["launched"] == 1


def test_retry_once_then_found(stub_cdp):
    stub_cdp["tab_results"] = [None, {"url": "https://florr.io/"}]
    retries = []
    flow.ensure_chrome_ready(None, confirm=lambda p: True,
                             prompt_retry=lambda p: retries.append(1) or True)
    assert len(retries) == 1
    assert stub_cdp["launched"] == 1


def test_retry_declined_raises(stub_cdp):
    stub_cdp["tab_results"] = [None]
    with pytest.raises(flow.ChromeSetupCancelled):
        flow.ensure_chrome_ready(None, confirm=lambda p: True,
                                 prompt_retry=lambda p: False)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_gui_chrome_flow.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'gui_chrome_flow'`

- [ ] **Step 3: 写实现**

Create `gui_chrome_flow.py`:

```python
"""GUI 版的专用 Chrome 引导 —— 把 cdp_bridge.launch_dedicated_chrome() 那套
命令行 input() 换成模态对话框. 逻辑(就绪就跳过 / 确认后杀掉重启 / 等 florr.io
标签页 / 没等到就问要不要重试)跟命令行版一一对应.

confirm 和 prompt_retry 做成可注入参数 —— 默认用 tkinter.messagebox, 单测传
假函数, 不弹真窗口.
"""
from tkinter import messagebox

import cdp_bridge


class ChromeSetupCancelled(Exception):
    """用户在引导流程里点了取消/否. 调用方(GUI 的开始按钮)应当静默中止启动."""


def _default_confirm(parent):
    return messagebox.askokcancel(
        "准备专用 Chrome",
        "即将关闭所有 Chrome 窗口以启动专用实例(未保存的标签页会丢失).\n继续?",
        parent=parent,
    )


def _default_prompt_retry(parent):
    return messagebox.askretrycancel(
        "还没检测到 florr.io",
        "没在专用 Chrome 窗口里检测到 florr.io 标签页.\n"
        "确认已经在那个新窗口里登录并打开了 florr.io, 然后点重试.",
        parent=parent,
    )


def ensure_chrome_ready(parent, *, confirm=_default_confirm, prompt_retry=_default_prompt_retry):
    """确保专用 Chrome 就绪; 用户取消则抛 ChromeSetupCancelled."""
    if cdp_bridge.is_dedicated_chrome_ready():
        return
    if not confirm(parent):
        raise ChromeSetupCancelled()
    cdp_bridge.quit_and_launch_chrome()
    while True:
        if cdp_bridge.wait_for_florr_tab(15) is not None:
            return
        if not prompt_retry(parent):
            raise ChromeSetupCancelled()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_gui_chrome_flow.py -v`
Expected: PASS（5 个）

- [ ] **Step 5: Commit**

```bash
git add gui_chrome_flow.py test_gui_chrome_flow.py
git commit -m "$(printf 'feat: add GUI Chrome bootstrap flow (modals, not console input)\n\nensure_chrome_ready mirrors launch_dedicated_chrome: skip if ready, else\nconfirm -> quit_and_launch -> wait for the florr.io tab -> offer retry.\nconfirm/prompt_retry are injectable so tests never open a window.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 7: `gui_app.py` —— 窗口外壳 + 侧栏 + AFK 开关 + 纯函数

**Files:**
- Create: `gui_app.py`
- Test: `test_gui_app.py`

**Interfaces:**
- Consumes: `app_config`（Task 1）、`afk_watch`（Task 3）、`customtkinter`
- Produces:
  - `gui_app.worker_command() -> list[str]` —— frozen 时 `[sys.executable, "--worker"]`；脚本时 `[sys.executable, "-u", <main.py 绝对路径>, "--worker"]`
  - `gui_app.build_worker_config(*, map_name, location, area, duration, short_limit, enemy_ai, auto_switch, afk) -> dict` —— 组装成 `app_config` schema 的 dict（`location`/`area` 传 `(x,y)` / `[(x,y),(x,y)]`，函数负责转成 list）
  - `gui_app.App(ctk.CTk)` —— 主窗口类（本任务只做外壳：侧栏 3 按钮 + 占位页 + 底部 AFK `CTkSwitch`）
  - `gui_app.main() -> None` —— `App().mainloop()`

- [ ] **Step 1: 写失败测试**

Create `test_gui_app.py`:

```python
import os
import sys

import gui_app


def test_worker_command_script_mode(monkeypatch):
    monkeypatch.setattr(gui_app.sys, "frozen", False, raising=False)
    cmd = gui_app.worker_command()
    assert cmd[0] == sys.executable
    assert cmd[1] == "-u"
    assert cmd[-1] == "--worker"
    assert cmd[-2].endswith("main.py")
    assert os.path.isabs(cmd[-2])


def test_worker_command_frozen_mode(monkeypatch):
    monkeypatch.setattr(gui_app.sys, "frozen", True, raising=False)
    monkeypatch.setattr(gui_app.sys, "executable", "/opt/florr/florr-auto-pathing")
    assert gui_app.worker_command() == ["/opt/florr/florr-auto-pathing", "--worker"]


def test_build_worker_config_shapes_values():
    cfg = gui_app.build_worker_config(
        map_name="ocean", location=(11, 22), area=[(1, 2), (3, 4)],
        duration=120, short_limit=3, enemy_ai=False, auto_switch=True, afk=True,
    )
    assert cfg == {
        "map": "ocean",
        "location": [11, 22],
        "farming_area": [[1, 2], [3, 4]],
        "farming_duration": 120,
        "consecutive_short_round_limit": 3,
        "enemy_ai_enabled": False,
        "auto_switch_server": True,
        "afk_enabled": True,
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_gui_app.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'gui_app'`

- [ ] **Step 3: 写实现**

Create `gui_app.py`:

```python
"""florr-auto-pathing 的控制面板 GUI. 无参跑 `python main.py` / 双击 exe 就进这里.

进程模型: 这个窗口不跑寻路逻辑 —— 点"开始"时它 (1) 用模态框引导好专用 Chrome
和 florr-auto-afk, (2) 把界面上的配置写进 config.json, (3) subprocess.Popen 一个
`main.py --worker` 子进程, 把它的 stdout 逐行灌进日志框. "停止"给子进程发信号让
它先 reset_keyboard() 再退出.
"""
import os
import signal
import subprocess
import sys
import threading

import customtkinter as ctk

import app_config
import afk_watch

_IS_WINDOWS = sys.platform == "win32"


def worker_command():
    """拉起 worker 子进程的命令行. frozen(PyInstaller)时 sys.executable 就是我们
    自己的 exe, 直接带 --worker; 脚本模式下要显式 python + main.py 路径, 加 -u 让
    子进程 stdout 行缓冲(日志实时进面板)."""
    if getattr(sys, "frozen", False):
        return [sys.executable, "--worker"]
    main_py = os.path.join(os.path.dirname(os.path.abspath(__file__)), "main.py")
    return [sys.executable, "-u", main_py, "--worker"]


def build_worker_config(*, map_name, location, area, duration, short_limit,
                        enemy_ai, auto_switch, afk):
    """界面上的值 -> app_config schema 的 dict. 坐标统一转成 list(JSON 里没有 tuple)."""
    return {
        "map": map_name,
        "location": [int(location[0]), int(location[1])],
        "farming_area": [[int(area[0][0]), int(area[0][1])],
                         [int(area[1][0]), int(area[1][1])]],
        "farming_duration": int(duration),
        "consecutive_short_round_limit": int(short_limit),
        "enemy_ai_enabled": bool(enemy_ai),
        "auto_switch_server": bool(auto_switch),
        "afk_enabled": bool(afk),
    }


class App(ctk.CTk):
    def __init__(self):
        super().__init__()
        self.title("florr-auto-pathing")
        self.geometry("880x560")
        ctk.set_appearance_mode("dark")

        self._cfg = app_config.load_config()
        self.proc = None
        self._reader = None

        self.grid_columnconfigure(1, weight=1)
        self.grid_rowconfigure(0, weight=1)

        # ---- 侧栏 ----
        side = ctk.CTkFrame(self, width=120, corner_radius=0)
        side.grid(row=0, column=0, sticky="nsew")
        side.grid_rowconfigure(4, weight=1)  # spacer 行
        self._pages = {}
        for i, name in enumerate(("控制台", "账号", "时间表")):
            btn = ctk.CTkButton(side, text=name, anchor="w",
                                command=lambda n=name: self._show_page(n))
            btn.grid(row=i, column=0, padx=10, pady=(10 if i == 0 else 4, 4), sticky="ew")
            if name != "控制台":
                btn.configure(state="disabled")  # 阶段2

        # 底部大 AFK 开关
        afk_box = ctk.CTkFrame(side, fg_color="transparent")
        afk_box.grid(row=5, column=0, padx=10, pady=14, sticky="ew")
        ctk.CTkLabel(afk_box, text="自动检测 AFK", font=("", 12)).pack()
        self.afk_switch = ctk.CTkSwitch(afk_box, text="", command=self._on_afk_toggle)
        self.afk_switch.pack(pady=4)
        if self._cfg["afk_enabled"]:
            self.afk_switch.select()
        if not _IS_WINDOWS:
            self.afk_switch.configure(state="disabled")
            ctk.CTkLabel(afk_box, text="(仅 Windows)", font=("", 9),
                         text_color="gray").pack()

        # ---- 内容区: 占位, Task 8 填控制台页 ----
        self.content = ctk.CTkFrame(self)
        self.content.grid(row=0, column=1, sticky="nsew", padx=12, pady=12)
        self._placeholder = ctk.CTkLabel(self.content, text="控制台(Task 8 填充)")
        self._placeholder.pack(expand=True)

    def _show_page(self, name):
        pass  # Task 8: 控制台是唯一可用页, 其余灰置

    def _on_afk_toggle(self):
        pass  # Task 10

    def on_closing(self):
        if self.proc and self.proc.poll() is None:
            self._stop_worker()
        self.destroy()


def main():
    app = App()
    app.protocol("WM_DELETE_WINDOW", app.on_closing)
    app.mainloop()
```

> `test_gui_app.py` 只 import 模块、调两个纯函数，不实例化 `App`（CI 无显示器）。`import customtkinter` 在无头环境能成功（只有 `ctk.CTk()` 才需要 `$DISPLAY`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_gui_app.py -v`
Expected: PASS（3 个）

- [ ] **Step 5: 手动冒烟**（有显示器的机器）

Run: `python main.py`
Expected: 弹出深色窗口，左侧「控制台/账号/时间表」三个按钮（后两个灰），左下「自动检测 AFK」开关（Mac 上灰 + 「(仅 Windows)」）。关窗口不报错。

- [ ] **Step 6: Commit**

```bash
git add gui_app.py test_gui_app.py
git commit -m "$(printf 'feat: add gui_app shell (sidebar, placeholder pages, AFK switch)\n\nCustomTkinter window with the sidebar nav and the bottom-left AFK\nswitch (disabled off-Windows). worker_command / build_worker_config are\nmodule-level pure functions and unit-tested; App itself is smoke-only.\n`python main.py` with no args now launches this.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 8: `gui_app.py` —— 控制台页（地图选择器 + 配置项 + 双向绑定）

**Files:**
- Modify: `gui_app.py`（`App` 里加控制台页控件；用 `MapPicker`；地图下拉；数字框 ↔ 选择器同步）
- Test: `test_gui_app.py`（无新纯函数可测；本任务靠手动冒烟）

**Interfaces:**
- Consumes: `gui_map_picker.MapPicker`（Task 5）、`app_config`（Task 1）
- Produces: `App` 上新增属性 `self.map_menu`、`self.picker`、`self.duration_entry`、`self.enemy_switch`、`self.short_entry`、`self.autoswitch_check`、`self.status_label`、`self.start_btn`；方法 `self._current_values() -> dict`（读控件 → 传给 `build_worker_config` 的 kwargs）

- [ ] **Step 1: 实现控制台页**

把 `App.__init__` 里的 `self.content` 占位块换成真正的控制台页。新增私有方法。关键点：

```python
        # ---- 控制台页 ----
        self.content = ctk.CTkFrame(self)
        self.content.grid(row=0, column=1, sticky="nsew", padx=12, pady=12)
        self.content.grid_columnconfigure(0, weight=3)
        self.content.grid_columnconfigure(1, weight=2)
        self.content.grid_rowconfigure(1, weight=1)

        self.map_menu = ctk.CTkOptionMenu(
            self.content, values=list(app_config._VALID_MAPS),
            command=self._on_map_change)
        self.map_menu.set(self._cfg["map"])
        self.map_menu.grid(row=0, column=0, columnspan=2, sticky="w", pady=(0, 8))

        from gui_map_picker import MapPicker
        self.picker = MapPicker(
            self.content,
            on_point_change=self._on_picker_point,
            on_area_change=self._on_picker_area)
        self.picker.grid(row=1, column=0, sticky="nsew", padx=(0, 10))

        right = ctk.CTkFrame(self.content, fg_color="transparent")
        right.grid(row=1, column=1, sticky="nsew")

        self.duration_entry = self._labeled_entry(right, "刷怪时长 (秒)",
                                                  str(self._cfg["farming_duration"]))
        self.enemy_switch = ctk.CTkSwitch(right, text="索敌 AI (YOLO 追击/规避)")
        self.enemy_switch.pack(anchor="w", pady=6)
        if self._cfg["enemy_ai_enabled"]:
            self.enemy_switch.select()
        self.short_entry = self._labeled_entry(
            right, "连续短局阈值", str(self._cfg["consecutive_short_round_limit"]))
        self.autoswitch_check = ctk.CTkCheckBox(right, text="连续没刷满自动换服务器")
        self.autoswitch_check.pack(anchor="w", pady=6)
        if self._cfg["auto_switch_server"]:
            self.autoswitch_check.select()

        self.log_box = ctk.CTkTextbox(right, font=("Menlo", 11), state="disabled")
        self.log_box.pack(fill="both", expand=True, pady=(8, 0))

        self.status_label = ctk.CTkLabel(self.content, text="状态：未运行", anchor="w")
        self.status_label.grid(row=2, column=0, columnspan=2, sticky="ew", pady=(8, 4))

        self.start_btn = ctk.CTkButton(self.content, text="▶ 开始", height=40,
                                       command=self._on_start_stop)
        self.start_btn.grid(row=3, column=0, columnspan=2, sticky="ew")

        # 初值灌进选择器
        self.picker.load_map(self._cfg["map"])
        self.picker.set_point(tuple(self._cfg["location"]))
        self.picker.set_area([tuple(p) for p in self._cfg["farming_area"]])
        self._point = tuple(self._cfg["location"])
        self._area = [tuple(p) for p in self._cfg["farming_area"]]
```

辅助方法：

```python
    def _labeled_entry(self, parent, label, initial):
        ctk.CTkLabel(parent, text=label, anchor="w").pack(anchor="w")
        e = ctk.CTkEntry(parent)
        e.insert(0, initial)
        e.pack(anchor="w", fill="x", pady=(0, 6))
        return e

    def _on_map_change(self, name):
        self.picker.load_map(name)
        self.picker.set_point(None)
        self.picker.set_area(None)
        self._point = None
        self._area = None
        self._log_line(f"已切到 {name}，请重新点目标点 / 框刷怪区\n")

    def _on_picker_point(self, pt):
        self._point = pt

    def _on_picker_area(self, area):
        self._area = [tuple(area[0]), tuple(area[1])]

    def _current_values(self):
        return dict(
            map_name=self.map_menu.get(),
            location=self._point,
            area=self._area,
            duration=self.duration_entry.get(),
            short_limit=self.short_entry.get(),
            enemy_ai=bool(self.enemy_switch.get()),
            auto_switch=bool(self.autoswitch_check.get()),
            afk=bool(self.afk_switch.get()),
        )

    def _log_line(self, text):
        self.log_box.configure(state="normal")
        self.log_box.insert("end", text)
        self.log_box.see("end")
        self.log_box.configure(state="disabled")
```

`_on_start_stop` 本任务先放最小骨架（Task 9 补全进程逻辑）：

```python
    def _on_start_stop(self):
        vals = self._current_values()
        if vals["location"] is None or vals["area"] is None:
            self._log_line("⚠️ 请先在地图上点目标点并框出刷怪区\n")
            return
        self._log_line(f"(Task 9 尚未接进程) 将用配置: {vals}\n")
```

- [ ] **Step 2: 跑现有测试确认没回归**

Run: `pytest test_gui_app.py test_app_config.py test_gui_map_picker.py -q`
Expected: PASS

- [ ] **Step 3: 手动冒烟**

Run: `python main.py`
Expected：控制台页出现地图图片；左键点一下 → 绿色十字；按住拖 → 蓝色矩形；滚轮 → 放大到 4x；改「刷怪时长」框里数字不报错；切地图下拉 → 换图 + 清标记 + 日志提示。点「▶ 开始」→ 日志打印一行 `将用配置: {...}`。

- [ ] **Step 4: Commit**

```bash
git add gui_app.py
git commit -m "$(printf 'feat: build the control page (map picker + config fields)\n\nMap dropdown, MapPicker (click target / drag farm area / scroll zoom),\nduration / short-round / enemy-AI / auto-switch controls, a read-only\nlog box and a start button. Values seed from config.json; picker and\nfields stay in sync. Start button is a stub until the next task.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 9: `gui_app.py` —— worker 生命周期（启停 / Popen / 日志线程 / 退出）

**Files:**
- Modify: `gui_app.py`（`_on_start_stop` 拆成 `_start_worker` / `_stop_worker`；日志读取线程；子进程退出回主线程）
- Test: `test_gui_app.py`（追加 `worker_command` 的 `creationflags` 相关断言仅在 Windows 有意义，跳过；本任务主要靠手动冒烟）

**Interfaces:**
- Consumes: `gui_app.worker_command()`、`gui_app.build_worker_config()`、`app_config.save_config()`
- Produces: `App._start_worker()`、`App._stop_worker()`、`App._pump_log(proc)`（线程体）、`App._on_worker_exit(code)`

- [ ] **Step 1: 实现进程逻辑**

```python
    def _on_start_stop(self):
        if self.proc and self.proc.poll() is None:
            self._stop_worker()
        else:
            self._start_worker()

    def _start_worker(self):
        vals = self._current_values()
        if vals["location"] is None or vals["area"] is None:
            self._log_line("⚠️ 请先在地图上点目标点并框出刷怪区\n")
            return
        try:
            short_limit = int(vals["short_limit"])
            duration = int(vals["duration"])
            assert short_limit >= 1 and duration > 0
        except (ValueError, AssertionError):
            self._log_line("⚠️ 时长 / 短局阈值必须是正整数\n")
            return

        cfg = build_worker_config(**vals)
        app_config.save_config(cfg)
        self._cfg = cfg

        kwargs = {}
        if _IS_WINDOWS:
            # 独立进程组: 这样"停止"发的 CTRL_BREAK 只打到 worker, 不会连带把 GUI 也中断
            kwargs["creationflags"] = subprocess.CREATE_NEW_PROCESS_GROUP
        else:
            kwargs["env"] = {**os.environ, "PYTHONUNBUFFERED": "1"}

        self.proc = subprocess.Popen(
            worker_command(), stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
            text=True, bufsize=1, **kwargs)
        self._log_line("—— worker 已启动 ——\n")
        self.start_btn.configure(text="■ 停止")
        self._reader = threading.Thread(target=self._pump_log, args=(self.proc,),
                                        daemon=True)
        self._reader.start()

    def _pump_log(self, proc):
        for line in proc.stdout:
            self.after(0, self._log_line, line)
            self.after(0, lambda l=line: self.status_label.configure(
                text="状态：" + l.strip()[:60]) if l.strip() else None)
        code = proc.wait()
        self.after(0, self._on_worker_exit, code)

    def _stop_worker(self):
        if not self.proc:
            return
        try:
            if _IS_WINDOWS:
                self.proc.send_signal(signal.CTRL_BREAK_EVENT)
            else:
                self.proc.terminate()
        except Exception as e:
            self._log_line(f"发送停止信号失败: {e}\n")
        self.after(3000, self._force_kill_if_alive)

    def _force_kill_if_alive(self):
        if self.proc and self.proc.poll() is None:
            self.proc.kill()
            self._log_line("—— worker 未响应, 已强制结束 ——\n")

    def _on_worker_exit(self, code):
        self._log_line(f"—— worker 结束 (退出码 {code}) ——\n")
        self.status_label.configure(text="状态：未运行")
        self.start_btn.configure(text="▶ 开始")
        self.proc = None
```

- [ ] **Step 2: 跑测试确认没回归**

Run: `pytest test_gui_app.py -q`
Expected: PASS

- [ ] **Step 3: 手动冒烟**（Windows 或已备好专用 Chrome 的 Mac）

1. `python main.py` → 点目标点 + 框区域 → 「▶ 开始」。
2. 若专用 Chrome 没就绪：Task 10 才接引导，这一步预期 worker 子进程自己打印 `❌ 专用 Chrome 未就绪` 后退出，GUI 日志显示「退出码 1」，按钮弹回「▶ 开始」。（就绪的话 worker 正常跑，日志滚动。）
3. worker 正常跑时点「■ 停止」→ 3 秒内日志出现 `worker 结束`，按钮回「▶ 开始」。
4. 直接关 GUI 窗口 → `on_closing` 先停 worker 再退出，无残留 `python` / `segment.exe` 进程（任务管理器 / `ps` 确认）。

- [ ] **Step 4: Commit**

```bash
git add gui_app.py
git commit -m "$(printf 'feat: run the worker as a subprocess with a log pump thread\n\nStart writes config.json and Popen(main.py --worker), streaming stdout\ninto the log box. Stop sends CTRL_BREAK (Windows, own process group) /\nSIGTERM (POSIX) so the worker resets the keyboard before exiting, with\na 3s force-kill fallback. Closing the window stops the worker first.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 10: `gui_app.py` —— AFK 开关接线 + 开始前跑 Chrome 引导

**Files:**
- Modify: `gui_app.py`（`_on_afk_toggle` 实体化；`_start_worker` 开头调 `gui_chrome_flow.ensure_chrome_ready` + 按开关状态 `_ensure_afk`）
- Test: `test_gui_app.py`（追加 `_ensure_afk` 的可测部分 —— 抽成能不实例化窗口就调的函数或用 stub；见下）

**Interfaces:**
- Consumes: `gui_chrome_flow.ensure_chrome_ready` / `ChromeSetupCancelled`（Task 6）、`afk_watch.is_florr_auto_afk_running` / `stop_florr_auto_afk` / `download_florr_auto_afk` / `ensure_florr_auto_afk_running` / `_EXE_PATH`（Task 3）
- Produces:
  - `gui_app.start_afk(*, exe_exists, running, confirm_download) -> str` —— 纯决策函数，返回 `"already"` / `"declined"` / `"downloaded"` / `"download_failed"` / `"started"`（不做真 I/O，供单测）
  - `App._ensure_afk()` —— 调 `start_afk(...)` 并执行副作用（下载 / `ensure_florr_auto_afk_running`）
  - `App._on_afk_toggle()` —— 开 → `_ensure_afk()`；关 → `afk_watch.stop_florr_auto_afk()`；两种都把 `afk_enabled` 落进 `config.json`

- [ ] **Step 1: 写失败测试**（追加到 `test_gui_app.py`）

```python
def test_start_afk_already_running():
    assert gui_app.start_afk(exe_exists=True, running=True,
                             confirm_download=lambda: pytest.fail()) == "already"


def test_start_afk_missing_exe_declined():
    assert gui_app.start_afk(exe_exists=False, running=False,
                             confirm_download=lambda: False) == "declined"


def test_start_afk_missing_exe_download_ok(monkeypatch):
    monkeypatch.setattr(gui_app.afk_watch, "download_florr_auto_afk", lambda: True)
    assert gui_app.start_afk(exe_exists=False, running=False,
                             confirm_download=lambda: True) == "downloaded"


def test_start_afk_missing_exe_download_fails(monkeypatch):
    monkeypatch.setattr(gui_app.afk_watch, "download_florr_auto_afk", lambda: False)
    assert gui_app.start_afk(exe_exists=False, running=False,
                             confirm_download=lambda: True) == "download_failed"


def test_start_afk_exe_present_not_running():
    assert gui_app.start_afk(exe_exists=True, running=False,
                             confirm_download=lambda: pytest.fail()) == "started"
```

加 `import pytest` 到 `test_gui_app.py` 顶部。

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest test_gui_app.py -v -k start_afk`
Expected: FAIL —— `AttributeError: module 'gui_app' has no attribute 'start_afk'`

- [ ] **Step 3: 写实现**

```python
def start_afk(*, exe_exists, running, confirm_download):
    """AFK 开关打开时的决策(纯函数, 副作用由调用方按返回值执行).
    already: 已在跑, 什么都不用做
    declined: exe 缺, 用户拒绝下载
    downloaded / download_failed: exe 缺, 下过了(成/败)
    started: exe 在, 需要调用方去 ensure_florr_auto_afk_running()
    """
    if running:
        return "already"
    if not exe_exists:
        if not confirm_download():
            return "declined"
        return "downloaded" if afk_watch.download_florr_auto_afk() else "download_failed"
    return "started"
```

```python
    def _persist_afk(self, enabled):
        cfg = app_config.load_config()
        cfg["afk_enabled"] = bool(enabled)
        app_config.save_config(cfg)
        self._cfg = cfg

    def _ensure_afk(self):
        if not _IS_WINDOWS:
            return
        from tkinter import messagebox
        outcome = start_afk(
            exe_exists=os.path.isfile(afk_watch._EXE_PATH),
            running=afk_watch.is_florr_auto_afk_running(),
            confirm_download=lambda: messagebox.askyesno(
                "下载 florr-auto-afk?",
                "没检测到 florr-auto-afk(处理 AFK 弹窗用). 现在下载? 约 350MB.",
                parent=self),
        )
        self._log_line(f"AFK: {outcome}\n")
        if outcome in ("started", "downloaded"):
            afk_watch.ensure_florr_auto_afk_running()  # 写 config + Popen + 等启动(可能卡几秒)

    def _on_afk_toggle(self):
        enabled = bool(self.afk_switch.get())
        if enabled:
            self._ensure_afk()
        else:
            afk_watch.stop_florr_auto_afk()
            self._log_line("AFK: 已停止 florr-auto-afk\n")
        self._persist_afk(enabled)
```

`_start_worker` 开头（在校验 location/area 之后、`Popen` 之前）加：

```python
        try:
            gui_chrome_flow.ensure_chrome_ready(self)
        except gui_chrome_flow.ChromeSetupCancelled:
            self._log_line("已取消(专用 Chrome 未就绪)\n")
            return
        if bool(self.afk_switch.get()):
            self._ensure_afk()
```

顶部 import 补 `import gui_chrome_flow`。

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest test_gui_app.py -v`
Expected: PASS（含 5 个 `start_afk` 用例）

- [ ] **Step 5: 手动冒烟**

- Mac：AFK 开关灰，`_ensure_afk` 直接 return；「开始」会先弹 Chrome 引导框（取消 → 日志「已取消」，按钮不变）。
- Windows：拨动 AFK 开关 → 若没装弹下载框；装了则 `segment.exe` 起来（任务管理器可见），`config.json` 里 `afk_enabled` 变 `true`；关掉开关 → `segment.exe` 消失，`afk_enabled` 变 `false`。

- [ ] **Step 6: Commit**

```bash
git add gui_app.py test_gui_app.py
git commit -m "$(printf 'feat: wire the AFK switch and pre-start Chrome bootstrap\n\nStarting the worker first runs ensure_chrome_ready (modal) and, if the\nAFK switch is on, ensures florr-auto-afk. The switch starts/stops\nsegment.exe and persists afk_enabled. start_afk is an extracted pure\ndecision function with unit tests; the side effects live in the App.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

### Task 11: 打包 + 文档

**Files:**
- Modify: `main.spec`（加 `customtkinter` 到 `collect_all` + `hiddenimports`；`EXE(console=False)`）
- Modify: `PACKAGING.md`（新入口 = GUI；`config.json` 位置；`console=False` 取舍）
- Modify: `README.md`（`main.py` 现在开的是 GUI，不再是直接开跑）

**Interfaces:**
- Consumes: 前面所有任务的成品
- Produces: 可打包的 spec + 更新的文档（无新代码接口）

- [ ] **Step 1: 改 `main.spec`**

`hiddenimports` 列表加 `"customtkinter"`：

```python
hiddenimports = [
    "pyautogui", "pyscreeze", "pymsgbox", "pytweening", "pygetwindow", "mouseinfo",
    "PIL", "PIL._tkinter_finder", "websocket", "certifi", "tkinter", "customtkinter",
]
```

`collect_all` 循环加 `customtkinter`（它的主题 JSON / 字体是 data 文件，不 collect 会运行时 `FileNotFoundError`）：

```python
for pkg in ("torch", "ultralytics", "cv2", "customtkinter"):
    d, b, h = collect_all(pkg)
    datas += d
    binaries += b
    hiddenimports += h
```

`EXE(...)` 里 `console=True` 改成 `console=False`，并更新那行注释：

```python
    console=False,   # 主入口是 GUI 窗口; worker 子进程的日志由 GUI 用 Popen(stdout=PIPE) 收, 不需要控制台
```

- [ ] **Step 2: 改 `PACKAGING.md`**

在说明入口的段落补：双击 exe 打开的是控制面板 GUI；点「开始」后 GUI 会 `--worker` 自我拉起真正跑寻路的子进程；界面配置存在 exe 同级 `config.json`（首次运行没有该文件时用内置默认值，等价于改造前 `main.py` 里那套硬编码）。`console=False` 之后 worker 日志只在 GUI 日志框里看，不再有独立黑框。

- [ ] **Step 3: 改 `README.md`**

把 `Go run main.py.` 附近改成：`python main.py` 打开控制面板 GUI（选地图 / 点目标点 / 框刷怪区 / 开关索敌 AI 和自动检测 AFK / 一键启停）。老的「直接开跑」行为现在等价于 GUI 点「开始」，或 `python main.py --worker`（需要先有就绪的专用 Chrome）。

- [ ] **Step 4: 全量回归**

Run: `pytest -q`
Expected: PASS（`test_app_config` / `test_gui_map_picker` / `test_gui_chrome_flow` / `test_gui_app` / `test_main_worker` / `test_main_smoke` / `test_cdp_bridge` / `test_afk_watch` / `test_overlay` / `test_enemy_detect` / `test_server_lookup` / `test_utils`）

- [ ] **Step 5: 手动冒烟（打包，Windows）**

Run: `build_windows.bat`
Expected: `dist\florr-auto-pathing\florr-auto-pathing.exe` 双击 → 出 GUI（无黑框）→ 点「开始」跑通一轮。

- [ ] **Step 6: Commit**

```bash
git add main.spec PACKAGING.md README.md
git commit -m "$(printf 'build: bundle customtkinter, make the exe windowed, update docs\n\nmain.spec collects customtkinter data and flips console=False now that\nthe entry point is the GUI (worker logs go to the log box via a pipe).\nPACKAGING.md / README.md describe the GUI entry and config.json.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Self-Review

**1. Spec coverage:**

| Spec 要点 | 对应任务 |
|---|---|
| `argparse`：无参→GUI，`--worker`→循环 | Task 4 (argparse) + Task 7 (gui_app.main) |
| `__main__` 循环体搬进 `run_worker(cfg)` | Task 4 |
| GUI 点开始：引导 → 写 config → Popen worker → 日志线程 | Task 9 (Popen/线程) + Task 10 (引导) |
| 停止：terminate/CTRL_BREAK → worker SIGTERM/SIGBREAK → reset_keyboard | Task 4 (handler) + Task 9 (信号发送) |
| worker 崩溃/退出 → 按钮弹回 | Task 9 (`_on_worker_exit`) |
| 直接 `--worker` 且 Chrome 未就绪 → 报错退出 | Task 4 (`run_worker` 开头) |
| `app_config.py`：load/save/coerce/DEFAULTS/CONFIG_PATH | Task 1 |
| `config.json` schema（8 键） | Task 1 (DEFAULTS) + Task 7 (build_worker_config) |
| `enemy_ai_enabled=false` → 跳过 enemy_detect | Task 4 (`_maybe_scan_enemies`) |
| `auto_switch_server` 门 | Task 4 (`run_worker`) |
| `afk_enabled` 仅 GUI 用、worker 不读 | Task 4（`_apply_worker_config` 不含它）+ Task 10（持久化） |
| `gui_map_picker.py`：MapPicker + 纯变换函数 | Task 5 |
| `gui_chrome_flow.py`：`ensure_chrome_ready` + `ChromeSetupCancelled` | Task 6 |
| `cdp_bridge` 拆 `is_dedicated_chrome_ready` / `quit_and_launch_chrome` / `wait_for_florr_tab` | Task 2 |
| `afk_watch` 加 `is_florr_auto_afk_running` / `stop_florr_auto_afk` / `download_florr_auto_afk` | Task 3 |
| 控制台页布局 A1（侧栏 + 地图选择器左 + 配置/日志右 + 状态行 + 大按钮） | Task 7 (侧栏 + AFK) + Task 8 (页内) |
| AFK 开关：Windows 启停 segment.exe / Mac 灰 / 下载确认 / 持久化 | Task 10 (+ Task 7 灰置) |
| 数字框 ↔ 选择器双向同步；切地图清标记 | Task 8 |
| `requirements.txt` + `main.spec` + `PACKAGING.md` 加 customtkinter / console=False | Task 5 (requirements) + Task 11 (spec/docs) |
| worker stdout `-u` / `PYTHONUNBUFFERED` | Task 7 (`worker_command`) + Task 9 (`env`) |
| 缩放整数 1–4×、中心跟指针、无独立平移 | Task 5 (`_on_wheel` + `_view`) |
| 测试：新模块各自测 + 现有全绿 | 每个任务的 test 步 + Task 11 全量回归 |
| 阶段2 占位（账号/时间表页灰置、不加 config 键） | Task 7 (灰置按钮) |

无未覆盖要点。

**2. Placeholder scan:** 所有步骤含实际代码 / 命令 / 期望输出。`MapPicker._on_release` 给了修正版全文（不是「类似上面」）。手动冒烟步骤给了具体点击序列和预期现象，非「测一下」。无 TBD/TODO。

**3. Type consistency:**
- `app_config.load_config()` 返回 dict，键名在 Task 1 定义，Task 4 `_apply_worker_config` / Task 7 `build_worker_config` 用的是同一组键名（`consecutive_short_round_limit` / `farming_area` / `enemy_ai_enabled` …），一致。
- `_maybe_scan_enemies(enemy_ai_enabled, now, last_enemy_scan, prev_decision) -> (decision, last_enemy_scan)` —— Task 4 定义与 `auto_farming` 调用点参数顺序一致。
- `cdp_bridge.wait_for_florr_tab(timeout, interval=1.0)` —— Task 2 定义，Task 6 `ensure_chrome_ready` 调用 `wait_for_florr_tab(15)`，一致。
- `gui_app.worker_command()` 返回 `list[str]`，Task 7 定义、Task 9 传给 `subprocess.Popen`，一致。
- `gui_app.start_afk(*, exe_exists, running, confirm_download)` —— Task 10 定义与测试、`_ensure_afk` 调用点关键字一致。
- `gui_map_picker.View` 字段 `s / offset_x / offset_y / img_w / img_h` —— Task 5 dataclass 定义、`image_to_widget` / `widget_to_image` / 测试三处用的字段名一致。
- `MapPicker(master, *, on_point_change, on_area_change)` + `.load_map/.set_point/.set_area` —— Task 5 定义、Task 8 实例化一致。

无签名/属性名漂移。

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-31-gui-phase1.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
